{
  "name": "claude-plugins-tooling",
  "private": true,
  "description": "Dev tooling for the claude-plugins repo. Not a published package; lint:md runs the suite's own dependency-free linter.",
  "scripts": {
    "lint:md": "scripts/lint-markdown.sh"
  }
}
