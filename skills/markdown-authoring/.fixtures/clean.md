---
name: clean
---

# Clean

A conformant file with `{literal}` braces in code and an autolink <https://example.com> plus <a@b.com>.

The label below:

- item one
- item two
    - nested at four
        1. deeper ordered
- item three

```js
const y = 2;
```

| a | b |
| - | - |

> a quote
> continues

A double-backtick span `` {x}: Bash: `cmd` `` is code — its braces and inner backticks stay unflagged.
