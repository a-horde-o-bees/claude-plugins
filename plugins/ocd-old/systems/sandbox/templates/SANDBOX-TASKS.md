# Sandbox: {feature-id}

One-line statement of what this sandbox is meant to resolve. Replace with the concrete end-state goal; keep it tight enough to read in one breath.

## Pointers

References to ROADMAP entries, idea logs, design docs, or other source material this sandbox draws from. Link by path; don't duplicate content here. The reader follows pointers when they need depth.

- (none yet)

## Tasks

The working entry point for this sandbox. Read at session start to know what's outstanding; update as work progresses. Add tasks as scope clarifies; check off as work completes. Checked items stay in the list rather than moving to a separate "done" section — history is read-once context.

- [ ] (first task)

## Open Questions

Decisions or scope clarifications still outstanding. Move to Tasks once resolved; remove the section entirely if none remain.

- (none yet)
