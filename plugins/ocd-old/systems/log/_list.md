# Log: List

List existing log entries grouped by type.

## Process

1. For each type directory in `logs/`:
    1. Glob: `*.md` excluding `_template.md`
2. Present entry titles grouped by type

## Report

- Entry count per type with titles
