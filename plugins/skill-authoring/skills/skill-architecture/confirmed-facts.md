# Confirmed facts about Claude Code platform behavior

**Empty — rebuild in progress.** No facts are confirmed under the current (sound) measurement mechanic yet.

The prior digest rested on a flawed method and was withdrawn (kept in `assertions/quarantine/` as reference until re-derived or dropped — do not cite it).

Facts land here one at a time as ladder rungs are validated. See [`assertions/README.md`](assertions/README.md).
