# Pass 3 Refinements — Bin 10

Pass 3 (Attempt 2) refinements to `_CONSOLIDATED_breadth-then-depth.md` from a second normalize cycle on the bin 10 samples. Samples were already in role-tree format from Pass 2; this pass verified alignment, scrubbed cross-corpus phrasings that compared the sample against unnamed peers ("rare among community MCP servers", "unusual among MCPs surveyed", "Contrarian vs the hatchling-dominated sample", "unusual among MCP servers that typically rely on MCP Inspector", "Tool editing capability is unusual for MCP servers", "One of the clearer uses of MCP resources rather than overloading tools", "unusually short for an MCP server", "note unusual `src.` prefix"), and re-surfaces unresolved structural concerns from Pass 2 that the reconciler has not yet integrated.

## Proposed new paths

> Format: `<role> > <new-path>` — supporting samples — qualitative description draft

(none — Pass 2 proposed three new paths; the reconciler integrated `Distribution channel > Multi-channel publication` but did not separate `Configuration delivery > Provider-specific proxy override hierarchy`, `Capability surface > Tool with output-shaping parameters (token economy)`, or `Capability surface > Embedded RAG with local-default backend`. These remain unresolved and are re-listed under Proposed description sharpenings since the reconciler may have judged them as sub-axes of existing paths rather than new sibling paths)

## Proposed description sharpenings

> Format: `<role> > <existing-path>` — what the existing description misses; supporting samples; sharpening suggestion

- `Configuration delivery > Environment variables` — `ppl-ai--modelcontextprotocol.md` exhibits a deliberate proxy-override precedence chain: `PERPLEXITY_PROXY` takes priority over standard `HTTPS_PROXY`/`HTTP_PROXY`. The provider-prefixed env var intentionally wins over the conventional system-wide ones, recognizing corporate/enterprise deployments where a server-specific proxy must override system defaults. Existing description doesn't surface intentional precedence chains where provider-prefixed env vars override standard names. Sharpening: note that some servers document precedence ordering between provider-prefixed env vars and conventional ones, particularly for proxy configuration in enterprise environments. Carried forward from Pass 2 unresolved.

- `Capability surface > Tools-only, hand-curated narrow surface` — `ppl-ai--modelcontextprotocol.md` exposes an optional `strip_thinking` parameter on the per-call API surface that removes reasoning tags from Perplexity output for token savings. This is a per-call output-shaping parameter for token economy, distinct from per-tool output format selection (which is about format choice; this is about content reduction). Existing description captures hand-curated narrow surfaces but doesn't surface output-shaping/token-economy parameters as a structural per-tool design choice. Sharpening: note that narrow-surface tool sets sometimes expose per-call parameters specifically for output verbosity reduction (token economy), distinct from format selection. Carried forward from Pass 2 unresolved.

- `Capability surface > Embedded RAG / retrieval pipeline` — `qdrant--mcp-server-qdrant.md` ships `fastembed` (ONNX-backed embedding lib) as a default-on, no-API-key local embedding backend, with optional escape hatch to remote services via `EMBEDDING_PROVIDER`. Existing description names embedded retrieval but doesn't surface the local-default-with-no-credentials onboarding stance as a structurally distinct choice (server runs out of the box without any embedding credential setup; remote embedding services are opt-in). Sharpening: distinguish "always remote embedding (requires keys)" from "local-default with optional remote" as a sub-axis; the no-API-key default lowers onboarding friction for vector-DB MCPs. Carried forward from Pass 2 unresolved.

- `Authentication > Cloud-native identity / credential chain` — `redis--mcp-redis.md` exhibits Azure EntraID with three explicit sub-flows (service principal, managed identity, default Azure credential) plus automatic token renewal with background refresh, layered as an *alternative* to the standard Redis ACL credential path. Existing description names "service principal, managed identity, default Azure credential" but doesn't surface the dual-path coexistence (cloud-native AND traditional credential side-by-side). Sharpening: note that cloud-native identity is often offered as an *alternative* path to the upstream's native auth, not a replacement — same server speaks both. Carried forward from Pass 2 unresolved.

- `Capability surface > Tools-heavy domain wrapper / domain-tool catalog` — `redis--mcp-redis.md` adds a sub-pattern: in-server documentation-search tool via separate HTTP endpoint configured by `MCP_DOCS_SEARCH_URL` — RAG-style documentation augmentation attached to a database server. Existing description captures large tool catalogs but doesn't surface the "domain wrapper plus docs-search-as-tool" cluster. Sharpening: note when the domain-wrapper additionally exposes an in-server docs-search endpoint as a tool surface, distinct from the database operations. Carried forward from Pass 2 unresolved.

- `Entry point and launch > Console script via [project.scripts] / npm bin` — `redis--mcp-redis.md` carries `[project.scripts]: redis-mcp-server = "src.main:cli"` with `src.` prefix in the module path. The `src.` prefix means the project's `src/` directory is itself imported as a top-level package rather than serving as a layout container — dotted module paths in console-script entry points typically point to top-level package names. Sharpening: note that some projects use `src.<module>:func` paths in `[project.scripts]` rather than a top-level module name, with implications for the import-resolution layout. Carried forward from Pass 2 unresolved.

- `Capability surface > User-publishable tools` — `riza-io--riza-mcp.md` exhibits an `edit_tool` operation that modifies existing saved tools at runtime (not just `create_tool` and `execute_tool`). Existing description likely captures the publish-and-execute pattern; the edit-after-publish capability is structurally distinct. Sharpening: note that tool-editing capability (`edit_tool`) is structurally distinct from publish-once-and-immutable patterns; mutable saved-tool surface is a separate design point under user-publishable tools. Carried forward from Pass 2 unresolved.

## Proposed new roles

> Format: `<new-role>` — what role this is, why it doesn't fit any existing role, supporting samples

(none — every fact in this bin maps to an existing role)

## Proposed bucket splits

> Format: `<role> > <existing-path>` — why it should split, into what, supporting samples

(none from this bin)

## Structural concerns

> Anything that's hard to fit cleanly under any role/path; questions for the reconciler

- **paypal `--tools=all` flag for capability gating.** Placed under `Capability surface > Capability gating flags (per-tool, per-category, write-mode)`, but the `--tools=all` shape is genuinely close to `Capability gating via tool subsets at install time` (the README pattern is "you opt in to all or selective subsets via the same flag at launch"). The boundary between "install-time capability flag" and "launch-time capability flag" is fuzzy when the `--tools` flag is on the launcher invocation (`npx -y @paypal/mcp --tools=all`); npx invocation is essentially launch-time but described as install in some samples. Reconciler may want to clarify the boundary or merge the two paths. Carried forward from Pass 2 unresolved.

- **paypal sandbox/production environment branch.** PayPal's `PAYPAL_ENVIRONMENT=SANDBOX|PRODUCTION` is a single-binary environment selector — sandbox/production routed by env var rather than separate entry points. This is a structural choice (single artifact serving multiple deployment shapes via env var) but doesn't fit cleanly under any role. Captured under `Configuration delivery > Environment variables` without escalating; reconciler may want to surface "environment-mode selector" as a sub-pattern of configuration delivery or multi-tenancy. Carried forward from Pass 2 unresolved.

- **ppl-ai HTTP mode CORS support.** The Perplexity server supports CORS on its HTTP transport for shared-server deployments. Currently absorbed into the Streamable HTTP description with a mention of CORS. Reconciler may want a separate path or sharpening for "HTTP mode with CORS for shared client access" since this is the operational forcing function for multi-client browser-reachable HTTP MCP servers. Carried forward from Pass 2 unresolved.

- **pragmar `--interactive` REPL.** pragmar's `--interactive` flag selects a terminal REPL mode rather than a transport. The existing path `Observability > --interactive REPL mode` — actually the consolidated has `Developer ergonomics > Inspector/debug tooling references` and a path under Transport `Selection mechanism` — not a direct REPL-mode path. Currently placed under `Transport > Selection mechanism` (noting that `--interactive` is not actually a transport) and under `Developer ergonomics > Inspector/debug tooling references`. Reconciler may want the Selection-mechanism description to acknowledge non-transport modes that bypass MCP entirely (REPL mode skips the protocol). Carried forward from Pass 2 unresolved.

- **qdrant CLI args deprecated.** README states "CLI args deprecated" — the env-var-only config posture is itself a deliberate choice (away from CLI flags toward env vars). Currently absorbed under `Configuration delivery > Environment variables`. Reconciler may want a sharpening: env-var-only as a deliberate posture distinct from accidentally-env-var-only when the project drifted that way. Carried forward from Pass 2 unresolved.

- **redis SSL granular knobs.** Redis MCP exposes `--ssl-ca-path`, `--ssl-keyfile`, `--ssl-certfile` alongside the URI scheme — granular SSL knobs as separate CLI flags. Currently captured under `Configuration delivery > CLI flags` without escalation; the granularity is a structural sub-choice (URI carries some info, SSL flags carry the rest). Reconciler may want to note the dual-channel configuration pattern (URI + sidecar flags) where some config layers above the connection string. Carried forward from Pass 2 unresolved.

- **reminia editable-install-only distribution.** Currently captured under `Distribution channel > Source clone with editable install` with the "developer-mode-as-release" annotation. The pattern is real (no PyPI release; expected user path is clone + editable install) but the existing path's framing is generic source-clone. Reconciler may want a description sharpening on `Source clone with editable install` to call out the case where it's the *only* install path (no PyPI parallel publication) — a deliberate distribution-strategy choice, not an oversight. Carried forward from Pass 2 unresolved.

- **redis `Anthropic Claude Agent SDK` mention.** Redis MCP README references "Anthropic Claude Agent SDK" but the actual SDK in dependencies is `mcp[cli]>=1.26.0` (raw MCP SDK), not Claude Agent SDK. The README phrasing is a marketing-style attribution rather than a technical dependency. redis is placed under `Server runtime > Python with raw MCP SDK` based on the actual `mcp[cli]>=1.26.0` dependency rather than the README phrasing. The Anthropic SDK reference is captured in the runtime description prose but not as a separate path. Reconciler should not double-count this. Carried forward from Pass 2 unresolved.

- **rohitg00 OAuth 2.1 mention vs. existing path framing.** rohitg00's README says "optional OAuth 2.1 (RFC 9728)" which exactly matches the existing `OAuth 2.x with issuer + JWKS (HTTP-mode bolt-on)` path including the RFC 9728 detail. Good fit; flagging that the existing path's description is well-tuned to capture this case. Carried forward from Pass 2 unresolved.

- **rohitg00 dual `Mounted file credentials` and `Delegated to upstream toolchain credentials`.** Same observation as the bin5 reconciler note about feiskyer/mcp-kubernetes-server — kubectl reads `~/.kube/config`, server doesn't auth on its own. rohitg00 placed under both paths because both fit. Reconciler may want a convention here (one canonical path with two facets) since the same pattern recurs across kubectl-class MCP servers. Carried forward from Pass 2 unresolved.

- **ppl-ai `strip_thinking` parameter not preserved in sample text.** The Pass 2 refinement report cites `ppl-ai--modelcontextprotocol.md` as exhibiting an optional `strip_thinking` parameter that removes reasoning tags from Perplexity output for token savings, and that fact drove the proposed `Capability surface > Tool with output-shaping parameters (token economy)` path. However, the sample text itself never mentions `strip_thinking` — the fact lives only in the refinement file. This pass did not synthesize the fact back into the sample (no source available to verify the parameter exists; the original sample's only Perplexity-tool content is the four-tool surface mapping to product tiers). Reconciler should treat the proposed-path's evidence as Pass-2-recorded-but-not-sample-attested; either re-research the sample to confirm the fact, drop the path proposal, or accept the Pass-2 refinement-report attestation as evidence on its own.

## Convergence assessment

The bin is **converged**. All sample level-2 and level-3 headings exactly match consolidated role/path names; the structural check (`ocd-run log research check`) passed for all eight samples. Pass 3 changes were narrow:

- Scrubbed cross-corpus phrasings from five samples — `redis--mcp-redis.md` (two sites: "rare among community MCP servers — reflects enterprise Azure deployment pressure" and "note unusual `src.` prefix in the module path"), `pragmar--mcp-server-webcrawl.md` (three sites: "unusual among MCPs surveyed and demonstrates that valid MCP servers need not talk to external services at all", "Contrarian vs the hatchling-dominated sample", "unusual among MCP servers that typically rely on MCP Inspector"), `riza-io--riza-mcp.md` (one: "Tool editing capability (`edit_tool`) is unusual for MCP servers"), `reminia--zendesk-mcp-server.md` (two sites: "One of the clearer uses of MCP resources rather than overloading tools for read access — splits read/write across resources/tools" and "unusually short for an MCP server, but unambiguous in the context"). Each replaced with self-describing prose that preserves the underlying fact.

No new paths needed beyond what Pass 2 already proposed. The structural concerns above are all carry-forwards awaiting reconciler decisions; none are blocking. Pass 4 should not be required for this bin.
