# Tasks — no baked install paths

- [x] 1.1 flatten_skills.py: refuse `${CLAUDE_SKILL_DIR}` in units bound for non-skill hosts; absolute rewrite removed; docstring updated
- [x] 1.2 Fixture test: dep with bundled-file reference errors into a non-skill host, still flattens relatively into a skill host
- [x] 2.1 Gates green; sync; checkpoint
